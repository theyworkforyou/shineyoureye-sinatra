<h1 class="page-title text-center"><%= @page.title %></h1>

<h3 class="text-center">Updated Members</h3>
<ul class="media-list media-list--places row">
    <% @updated_states_legislature.each do |place| %>
        <li class="col-lg-4 col-md-4 col-sm-6 col-xs-12">
            <div class="media states_and_legislature">
                <div class="media-left">
                    <a href="<%= place.place_url %>">
                    <img class="media-object" src="<%= place.thumbnail_url %>" alt="place.name">
                    </a>
                </div>
                <div class="media-body">
                    <a href="<%= place.place_url %>"><h3 class="media-heading"><%= place.name %></h3></a>
                    <div class="kind">
                    <p><%= @page.legislature_name %></p>
                    <% if place.child_area? %>
                    <p><%= @page.legislature_name %> <%= @page.current_term_start_year %>-current</p>
                    <% end %>
                    </div>
                </div>
            <div>
        </li>
    <% end %>
</ul>

<h3 class="text-center">Other Members</h3>
<p class="text-center">We only have names of the state representatives in the states listed below, we do not have the contact details.</p>
<ul class="media-list media-list--places row">
    <% @incomplete_states_legislature.each do |place| %>
        <li class="col-lg-4 col-md-4 col-sm-6 col-xs-12">
            <div class="media states_and_legislature">
                <div class="media-left">
                    <a href="<%= place.place_url %>">
                    <img class="media-object" src="<%= place.thumbnail_url %>" alt="place.name">
                    </a>
                </div>
                <div class="media-body">
                    <a href="<%= place.place_url %>"><h3 class="media-heading"><%= place.name %></h3></a>
                    <div class="kind">
                    <p><%= @page.legislature_name %></p>
                    <% if place.child_area? %>
                    <p><%= @page.legislature_name %> <%= @page.current_term_start_year %>-current</p>
                    <% end %>
                    </div>
                </div>
            <div>
        </li>
    <% end %>
</ul>